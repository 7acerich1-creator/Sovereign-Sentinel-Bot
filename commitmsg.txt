fix: Pollinations.ai as primary image gen (FREE, no auth, unlimited)

Gemini is in debt, OpenAI is at -$0.06. Both paid image providers
are dead. Pollinations.ai is now PRIMARY for faceless factory scene
images — zero cost, no API key, no rate limits.

Chain: Pollinations (free) -> Imagen 4 (fallback) -> DALL-E 3 (fallback)
Pipeline now runs on $0.00 total: Groq LLM + Pollinations images.

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
