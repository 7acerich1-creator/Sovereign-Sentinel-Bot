# System Audit — 2026-04-25 (Ace going dark, bots autonomous)

**Auditor:** Sentinel Bot cowork session, ran the project_system_audit_playbook cold.
**Duration of audit:** ~30 min, no production state mutated.
**HEAD at audit start:** `fedc30d` (S114q, on origin/main).
**Bot live URL health:** `200 OK`, uptime 10,523s (~2.9h, stable). Pinecone ✓, Supabase ✓, Gemini ✓.

> The audit ran against live runtime — bot, Supabase, Vercel landing, GitHub. Everything below is verified, not recalled. Stale memory entries marked where relevant.

---

## 🟢 Healthy

**Content production (text/image)** — Anita's daily 18:30 UTC `dailyContentProduction` is firing on schedule. Last 4 days: 6 drafts/brand/day, both brands, no gaps. 12 fresh drafts/day, exactly the design target.

**Bluesky / LinkedIn / Threads (Buffer rail)** — last 5 days, per brand:
- Containment Field: Bluesky **51 ✅ / 2 ❌** (96% success)
- Sovereign Synthesis: LinkedIn **47 ✅ / 1 ❌** (98%), Threads **47 ✅ / 1 ❌** (98%)
- Channel-routing per brand is correct (TCF→Bluesky, SS→LinkedIn+Threads, no cross-leak).

**Yuki YouTube comment auto-reply (S115 ship)** — verified live. 42 comments seen lifetime, 11 in last 5 days, 1 successful reply at `2026-04-25 05:31:32 UTC` (2 sec after seeing the comment). Most-recent watcher run 05:31:30 UTC. The full attention→reply loop is producing data.

**Mission Control telemetry tables** — `landing_analytics` last fetched 06:00 UTC today (daily Vercel pull working). `youtube_analytics` 167 rows, last write 14:00 UTC Apr 24. `email_events` 2 rows in last 5d (Resend webhook firing).

**Sapphire infra (S114)** — 4 of 5 sapphire_* tables populated as designed. OAuth refresh tokens in `sapphire_credentials` (3 rows). Reminders queue (2), known facts (3), family profiles (2), news sources (3) all live. Two-lane Pinecone separation is in code (`sapphire-personal` vs `brand`).

**Bot dispatch loop** — 45 dispatches in last 5d, 40 completed, 5 failed = **89% completion**. Most agents at 100% (Veritas, Sapphire, Anita). Average duration 60-180s/dispatch.

**Repo cleanliness** — 0 TODO/FIXME comments in src. 0 Make.com refs (S52 purge stuck). 0 LIVE_STATE refs in src (S113+ retirement stuck). Only 2 "Ace Richie" refs left in src, both deliberately scoped as `legacy` markers in `youtube-stats-fetcher.ts` for backward-compat with the YT analytics row schema. No stale env var references that contradict master ref Section 10.

**Git state** — HEAD `fedc30d`, branch `main`. The phantom "all-D" output from sandbox `git status` is the known CRLF/mount artifact (per `feedback_crlf_noise_is_not_a_real_diff.md`) — not a real state. Repo is clean from a Windows-side check.

---

## 🟡 Drift / Watch (no auto-fix — surface to Ace)

**1. Facebook publishing — verified at 01:14 UTC, UNVERIFIED since.** The S115b page-token resolver shipped at commit `bc268e9`. 4 successful FB direct posts logged at the ship moment (3 TCF + 1 SS). **Zero new sweeps have run since the ship** — the next regular distribution sweep window is 12:00 UTC and 19:00 UTC today (Apr 25). All ❌ FB errors in the queue are from BEFORE the ship at 23:55 UTC Apr 24 (the old "publish_to_groups" failure pattern). The first real production proof of the fix is the next 12:00 UTC sweep.
- **Ace action when back:** check `content_engine_queue` for `posted_at > 2026-04-25 12:00 UTC` and confirm `buffer_results LIKE '%✅ facebook_direct%'`.

**2. Vector recurring `daily_metrics_sweep` failure.** Same agent, same task type, failed Apr 23 (`stasis_self_check`) AND Apr 24 (`daily_metrics_sweep`) with `⚠️ Agent loop reached maximum iterations without a final response`. Pattern: tool-loop hitting the iteration cap on Stripe metrics fetch. Vector is the ONLY agent with a recurring failure pattern in the last 5 days. Other agents 100% complete.
- **Suspect cause:** Vector's tool chain (StripeMetrics → buffer_analytics → social_scheduler) is making more tool calls than `iterCap` allows. Either raise the cap for Vector specifically, or shrink the daily_metrics_sweep prompt to fewer required reports.

**3. YouTube Analytics — retention/CTR fields are all zeros.** `youtube_analytics` has 167 rows with non-zero `views` (avg 65 SS, 109 TCF) but `retention=0` and `ctr=0` on every row. The S114 fix-B `youtube-stats-fetcher.ts` was supposed to populate these via the YT Analytics API v2. The first run was supposed to log either "retention patched X/Y videos" OR "OAuth tokens missing yt-analytics.readonly scope".
- **Most likely:** OAuth tokens were granted with `youtube.readonly` only (not `yt-analytics.readonly`). Re-consent required (Google Cloud Console, ~5 min).
- **Impact:** the Aesthetic Performance tile on Mission Control will render but every CTR/retention cell will read 0.0% — false signal that every aesthetic is failing. Tile becomes misleading until re-consent + first patch.

**4. 30-video A/B/C performance test — 0/30 shipped, pipeline IS NOT autonomous.** The S113+ rotation logic is in `faceless-factory.ts` (verified — `pickNextAesthetic` + `recordNicheRun` + the `AESTHETIC_MODIFIERS` constant). BUT `produceFacelessBatch` is **only triggered manually via `POST /api/faceless/produce`** (line 3102 of `src/index.ts`). There's no scheduled job that drives video production. Last `niche_cooldown` row: Apr 19 16:40 UTC (6 days ago). All 21 existing rows have `aesthetic_style = NULL` (predate S113+).
- **Implication:** while Ace is dark, the bots will NOT auto-produce videos. The 30-video test won't progress unless someone hits the webhook.
- **NOT a regression** — this matches how the system was designed. But the audit playbook ranks the 30-video goal as "the first business goal on Mission Control" and "stalls if not moved in 10+ sessions" — it's been stalled since Apr 19.

**5. `cta_audit_proposals` task approval poller broken.** 10 proposals all `status='approved'` (reviewed by Ace 2026-04-22 06:58-06:59 UTC). Per the MC handoff (S52 cross-sync log entry), the bot is supposed to poll for `status='approved'`, run `youtube_update_metadata` + `youtube_pin_comment`, then flip to `status='executed'`. **Three days later, all 10 are still `approved`, none `executed`.** The poller is either not registered, silently failing, or mismatched on the status filter.
- **Ace action:** grep `src/index.ts` for "cta_audit_proposals" — if no scheduler entry exists, that's the gap. Re-grant approvals after fix to re-trigger.

**6. Knowledge nodes / business learning loop — 0 writes in 5 days, last write Apr 14.** S114p shipped `insight-extractor.ts` to extract one insight per completed dispatch and write to that agent's Pinecone namespace (and optionally `shared`). The CODE is in place (153 lines, namespace-aware, gemini-flash extractor). But `knowledge_nodes` table in Supabase has 1 lifetime row, most recent Apr 14 (PRE-S114p ship). 40 dispatches completed in last 5d should have produced ~40 insight writes if cross-cutting, or at minimum populated agent-namespace counters.
- **Suspect:** the loop writes to Pinecone namespaces (correct per design) but the `knowledge_nodes` Supabase mirror was never wired. Or the function silently swallows errors. Need a Railway log search for `[InsightExtractor]` lines to confirm.

**7. RunPod / long-form video pipeline — dormant.** `niche_cooldown` flat-lined Apr 19. `backlog_distributed` flat-lined Apr 22. `rechop_completed` 10 lifetime rows. Pod cold for 6 days. **Costs nothing while idle**, but the long-form output rate is 0 videos/week. Per master ref + NORTH_STAR, faceless video is metric #1 (top-of-funnel attention) — and metric #1 is at 930/wk vs 10,000/wk target. Long-form pipeline being dormant is the root cause of metric #1 stagnation.

**8. NORTH_STAR action item is now stale.** `NORTH_STAR.md` line 217 says `Action: SYSTEM-WIDE AUDIT (next session)`. That's THIS audit. After Ace reads this report, NORTH_STAR's "Current Highest-Leverage Action" needs to be updated to the next item.

**9. Stale top-level repo docs.**
- `PROJECT_POD_MIGRATION.md` — 75KB, last touched Apr 18. Phase 6 closed at S79. The plan is COMPLETE per the master reference but the file still sits at repo root. Candidate for `archive/` move OR delete.
- `AGENT-BRAND-UPDATE-PROMPTS.md` — Apr 24, 5.6KB. S107 brand-overhaul artifact. Brand collapse (`Ace Richie` retired) was completed S113. File is leftover. Move to archive.
- `soul.md` — Mar 26, 26 lines. Pre-master-ref artifact. Identity is now in `personalities.json` + `shared-context.ts`. Move to archive or delete.

---

## 🔴 Failures (operational risk while Ace is dark)

**1. Mission Control git repo not where master ref says it is.** Master ref Section 3 lists `C:\Users\richi\Sovereign-Mission-Control` as the MC repo path. Reality:
- `C:\Users\richi\Sovereign-Mission-Control\` exists with .md files but is **not a git repo** (`fatal: not a git repository`).
- `C:\Users\richi\Sovereign-Mission-Control\repo\` contains a `NORTH_STAR.md`, `LIVE_STATE.md`, `CLAUDE.md`, `AGENTS.md`, `README.md` — but is also not a git checkout.
- Vercel is auto-deploying from somewhere; the canonical clone path is now unknown to this audit.
- **Risk:** if Ace has been editing MC from a different path, future Sentinel sessions that follow the master ref will silently miss those edits. Master ref Section 3 needs a correction once Ace confirms the real path.

**2. The 5 NORTH_STAR input metrics are still effectively at zero.** Re-measured at audit time:
- `initiates` table: **0 rows lifetime.** Diagnostic page is live (S57), wired correctly, but no leads have come through.
- `revenue_log`: 0 rows. **$0 revenue** through the audit window.
- `member_access`: 0 rows. No paid tiers.
- `landing_analytics` last 7d: 24 visitors total across 4 pages. Target is 500/wk = ~17x gap.
- `sovereign_metrics` table: **0 rows.** The dashboard's "current liquid sum" widget reads from an empty table. Either the writer never seeded, or it was never wired.
- This is the bottom-line truth the audit must surface: **the system is operationally healthy, but it has produced zero revenue across 115+ sessions.**

---

## Posting volume — last 5 days

| Brand | Posted | Partial | Failed | Expired |
|---|---|---|---|---|
| Containment Field | 10 (lifetime, 4 in last 24h) | 17 | — | 33 |
| Sovereign Synthesis | 11 (lifetime, 1 in last 24h) | 17 | — | 30 |

**Note:** "Posted" status in the queue requires ALL channels to succeed. The 17 partial drafts/brand are sweeps where Bluesky/LinkedIn/Threads succeeded but Facebook failed (pre-S115b ship). After 12:00 UTC sweep today, those should resolve to `posted` if FB stays healthy.

---

## Facebook health (S115b regression check)

| Check | Result |
|---|---|
| Page-token resolver code in `facebook-publisher.ts` | ✅ Lines 52-76, 24h cache, fallback-safe |
| Verified posts at ship moment (01:14 UTC) | ✅ 4 real PAGE_ID_POST_ID format IDs in `buffer_results` |
| New ❌ FB errors AFTER ship | 0 |
| New ✅ FB successes AFTER ship | 0 (no sweep has run) |
| Verdict | **PROVISIONALLY GREEN — first real test is 12:00 UTC sweep today** |

---

## Action items for Ace when back online (priority order)

1. **Verify FB sweep at 12:00 UTC** — query `content_engine_queue` for `posted_at > 2026-04-25 12:00:00 UTC AND buffer_results LIKE '%✅ facebook_direct%'`. If empty after 12:30 UTC, S115b regressed.

2. **Re-consent YouTube Analytics scope.** Google Cloud Console → OAuth credentials → re-grant with `yt-analytics.readonly` added. Then watch Railway logs for `[YTStatsFetcher] retention patched X/Y videos`. Without this, the 30-video A/B/C test cannot conclude.

3. **Wire a scheduler entry for `produceFacelessBatch`.** The 30-video goal is autonomous-blocked because video production only fires via manual webhook. Decision needed: cron 1x/day per brand, OR keep manual until Alfred-driven trigger ready?

4. **Fix the cta_audit_proposals approval-poller gap.** 10 approvals sat for 3 days. The poller either isn't registered or has a status-string mismatch. Quick fix would unlock 10 YouTube metadata updates.

5. **Diagnose Vector iteration-cap failures** on `daily_metrics_sweep`. Either raise `iterCap` for Vector specifically or shrink the sweep's tool requirements. Currently lose ~1 metrics report/day.

6. **Confirm `insight-extractor` is actually writing.** Railway log grep for `[InsightExtractor]` over last 5d. If silent, the loop is broken — and the "knowledge_nodes had 1 row in 11 days" stagnation Master Reference Section "SAPPHIRE" item 9 says we fixed is, in fact, NOT fixed.

7. **Repo lean-up:** move `PROJECT_POD_MIGRATION.md`, `AGENT-BRAND-UPDATE-PROMPTS.md`, `soul.md` to an `archive/` folder. Master reference still cites them in places where they no longer apply.

8. **Confirm Mission Control repo path.** Master ref Section 3 is wrong about where the MC repo lives. Once you confirm the right path, patch the master ref.

9. **NORTH_STAR update.** Replace "SYSTEM-WIDE AUDIT (next session)" with the next highest-leverage action — almost certainly **revenue activation, not infra:** the system is healthy enough; the bottleneck is now metric #1 (attention) and metric #2 (visitor volume) being orders of magnitude below target.

---

## What this audit DID NOT modify

- Zero production writes (no manual sweep, no token rotations, no schema changes).
- Zero pushes to `main`.
- Zero Railway env var edits.
- Zero stale-draft auto-expirations.
- Per playbook guardrails — every finding above is for Ace to triage, not for the audit session to silently resolve.
